const Home = () => {
    return (
        <main>
             <section className="">
                {posts.map((post) => (
                    <article>
                        
                    </article>
                ))}
            </section>
        </main>
    );
}
 
export default Home;